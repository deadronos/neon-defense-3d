/**
 * Legacy Tailwind JS config
 *
 * This project now prefers the Tailwind v4 CSS-first workflow.
 * Theme variables and design tokens are defined in index.css with @theme.
 * Keep this file empty for backwards compatibility.
 */
module.exports = {};
